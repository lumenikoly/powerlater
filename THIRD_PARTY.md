# Third-party notices

Power Timer uses Rust crates distributed through crates.io. The direct runtime dependencies are:

- Slint — GPL-3.0-only OR Slint Royalty-free License 2.0 OR Slint Software License 3.0. This application uses Slint under its royalty-free license.
- chrono — MIT OR Apache-2.0.
- windows-rs (Windows only) — MIT OR Apache-2.0.
- libc (Linux only) — MIT OR Apache-2.0.

The complete, reproducible dependency graph and exact versions are recorded in `Cargo.lock`. Source distributions downloaded by Cargo include their respective license files.
